<Window x:Class="UserActivity.Views.MainView"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:UserActivity.Views"
        mc:Ignorable="d"
        Title="Users Activity" Height="450" Width="800">
    <Grid Width="750" Height="400">
        <DataGrid ItemsSource="{Binding Path=gridView}" CanUserAddRows="false" AutoGenerateColumns="false" IsReadOnly="true" Margin="0,0,0,80">
            <DataGrid.Columns>
                <DataGridTextColumn Header="Data" Binding="{Binding date}"/>
                <DataGridTextColumn Header="Login" Binding="{Binding Path = login}"/>
                <DataGridTextColumn Header="Czy zalogowany w danym dniu (TAK/NIE)" Binding="{Binding Path = wasLoggedThatDay}" CanUserSort="false"/>
                <DataGridTextColumn Header="Łączny czas aktywności w danym dniu" Binding="{Binding Path = activityTime}" CanUserSort="false" Width="*"/>
            </DataGrid.Columns>

        </DataGrid>

        <TextBox Height="20" Margin="0,340,142,30" BorderBrush="Black" IsReadOnly="true"/>
        <Button x:Name ="LoadButtonClick" Content="Wczytaj plik" HorizontalAlignment="Left" Margin="641,346,0,0" VerticalAlignment="Top" Width="99"/>

    </Grid>
    
</Window>
